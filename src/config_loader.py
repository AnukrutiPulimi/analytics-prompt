"""Load and validate configuration for the analytics automation tool.

This module is responsible for reading JSON configuration files such as URLs,
selectors, and thresholds, then exposing them to the rest of the application.
"""


def load_urls_config(path):
    """Load the URLs configuration from the given file path."""
    pass


def load_selectors_config(path):
    """Load the selectors configuration from the given file path."""
    pass


def load_thresholds_config(path):
    """Load the thresholds configuration from the given file path."""
    pass
